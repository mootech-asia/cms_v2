# CMS v2 Handoff - 2026-07-16

> Branch: `main`  
> Application: `frontend/` (Nuxt 4 + PrimeVue + TailwindCSS + Pinia)  
> Status: code changes are committed directly to GitHub; GitHub build and static generation pass, while visual QA and deployment are still pending.

## Important repository state

- `main` contains the Nuxt delivery application under `frontend/`.
- The currently published URL, `https://mootech-asia.github.io/cms_v2/`, still serves the frozen legacy static site from `gh-pages`.
- Do not modify or delete `gh-pages`, and do not restore automatic main-to-pages mirroring, unless the owner explicitly approves the Nuxt deployment cutover.
- The work below is implemented only in the Nuxt application on `main`.

## Completed in this handoff

### 1. Deposit gateway and payment method flow

File: `frontend/app/pages/deposit.vue`

Added four gateway choices:

- Payment Gateway Provider A
- Payment Gateway Provider B
- Payment Gateway Provider C
- Payment Gateway Provider D

Payment methods, in order:

1. Bank Card
2. LinePay
3. USDT-TRC20
4. USDT-ERC20

Provider visibility rules:

| Provider | Visible methods |
|---|---|
| A | All four methods |
| B | Bank Card, LinePay, USDT-TRC20 |
| C | Bank Card, LinePay |
| D | Bank Card only |

Behavior:

- Bank Card and LinePay use the standard deposit form and transfer step.
- USDT-TRC20 and USDT-ERC20 use the crypto deposit form.
- The old standalone `USDT TRC20` badge inside Deposit Info was removed.
- Provider/method rows wrap to two columns on small screens.
- If the selected method becomes unavailable after switching providers, selection resets to the first available method.

Submission modal mapping:

| Provider | Result |
|---|---|
| A | `Success!` |
| B | `The maximum number of application attempts has been reached. Please contact customer support for assistance.` |
| C | `Your application has been temporarily suspended due to too many failed transaction attempts. Please contact customer support for assistance.` |
| D | `The requested amount has reached the maximum limit. Please contact customer support for assistance.` |

Relevant commits:

- `e3c12b7` Add gateway-specific deposit methods and result messages
- `663a23b` Keep deposit option rows responsive

### 2. Bank account deletion confirmation

Files:

- `frontend/app/pages/account.vue`
- `frontend/app/components/MemberModal.vue`
- `frontend/app/assets/css/main.css`
- `frontend/app/stores/bank.ts` (existing `removeAccount` action is now used)

Completed behavior:

- Clicking the trash icon opens a destructive confirmation modal.
- Modal content matches the supplied reference:
  - red trash icon
  - title: `Delete Account?`
  - dynamic bank name
  - text: `This action cannot be undone.`
  - left button: `Cancel`
  - right red button: `Delete`
- Cancel closes the modal without deleting.
- Delete calls `bankStore.removeAccount(index)`.
- Successful deletion shows `Bank account deleted successfully.`

Relevant commits:

- `dd83358` Add danger account deletion modal
- `e48d3d8` Link nickname editing and refine bank deletion
- `9fd0648` Style destructive bank account confirmation

### 3. Change nickname page

Files:

- `frontend/app/pages/change-nickname.vue`
- `frontend/app/pages/account.vue`
- `frontend/app/components/AppIcon.vue`
- `frontend/app/components/MemberHeader.vue`
- `frontend/app/components/MemberSidebar.vue`
- `frontend/app/components/MemberMenuDrawer.vue`
- `frontend/app/components/MobileBottomNav.vue`

Completed behavior:

- Account Overview now shows a pencil button beside the nickname.
- The button opens `/change-nickname`.
- The page shows current nickname, new nickname input, validation, and Submit.
- Validation requires 2-20 characters and a value different from the current nickname.
- Success modal text: `Nickname updated successfully.`
- After confirmation, navigation returns to `/account`.
- Updated nickname is shared through Nuxt `useState('member:nickname')` and appears in Account Overview and the member mobile header.
- Sidebar, drawer, and mobile member navigation keep Account Overview active while on `/change-nickname`.

Relevant commits:

- `9ec7ac7` Add change nickname page
- `534443d` Show updated nickname in member header
- Navigation/icon support commits: `ddeaf80`, `1c7db1b`, `e340109`, `3d9cf96`

## Verification already performed

- Re-fetched all modified files from GitHub `main`.
- Confirmed the new route file exists.
- Confirmed delete modal copy, dynamic bank subject, destructive styles, and delete action wiring.
- Confirmed nickname link, shared state, success copy, and active navigation mappings.
- No local checkout was used for implementation.

## Still pending

1. GitHub Actions validation is complete:

   - Workflow: `Frontend checks`
   - Node: `22.18.0`
   - `npm ci`: passed
   - `npm run build`: passed
   - `npm run generate`: passed

2. Perform visual QA at desktop and mobile widths:

   - `/deposit`
   - `/account`
   - `/change-nickname`

3. Test every Deposit provider:

   - A shows 4 methods and Success.
   - B shows 3 methods and its warning.
   - C shows 2 methods and its warning.
   - D shows 1 method and its warning.

4. Test deletion:

   - Cancel preserves the account.
   - Delete removes only the selected account.
   - Index/count remains correct after deleting the last visible account.

5. Test nickname behavior:

   - Submit stays disabled for empty, unchanged, shorter than 2, or longer than 20 characters.
   - Successful change updates Account Overview and member mobile header.
   - Refresh currently resets the nickname because `useState` is only in-memory.

6. Persistence/API integration is not implemented:

   - Deposit result messages are UI simulations based on selected provider.
   - Bank deletion uses the Pinia mock store.
   - Nickname uses in-memory Nuxt state.
   - Replace these placeholders with API calls when backend contracts are available.

7. Deployment decision remains open:

   - The public GitHub Pages URL still uses the legacy `gh-pages` branch.
   - Do not deploy the Nuxt output or alter `gh-pages` without explicit owner approval.

## Recommended next Claude action

1. Read this file and `CLAUDE.md`.
2. Run build/generate.
3. Fix compile errors, if any, without changing the requested behavior.
4. Start the Nuxt server and visually verify the three routes at desktop and mobile sizes.
5. Report screenshots/findings before any `gh-pages` deployment decision.


## Addendum: UI Kit module variants v1-v10

Files:

- `frontend/app/config/blocks.ts`
- `frontend/app/assets/css/main.css`

Completed behavior:

- All 10 registered block types now expose `v1` through `v10` in the UI Kit, for 100 selectable module/variant combinations.
- Existing `v1`-`v3` component files and their behavior remain unchanged.
- `v4`-`v10` are generated with `tenVariants()`; attributes, props, events, and slots are forwarded to the existing compatible content templates.
- The seven added visual systems are:
  - v4 Glass ledger
  - v5 Signal rail
  - v6 Chain grid
  - v7 Layered stack
  - v8 Terminal strip
  - v9 Protocol frame
  - v10 Network console
- Banner, ticker, sports, game rails, promotion cards, category hero, member card, header, and footer each receive module-specific treatments.
- New CSS uses only existing semantic skin variables. No hardcoded brand colors or scoped component CSS were added.
- Existing data stores and content fields were not changed.

Relevant commits:

- `02f7b7d` Register ten variants for every block
- `450f27f` Add seven modern Web3 block layouts
- `344fefb` Stabilize generated wrapper component names
- `be6c0c8` Align Web3 styles with semantic skin variables
- `ce85175` Label the ten UI Kit design systems

Verification performed:

- Re-fetched `blocks.ts` from GitHub `main`.
- Confirmed all 10 block definitions call `tenVariants()`.
- Confirmed the generated registry contains exactly `v1`-`v10`.
- Audited the new CSS variables; only `--c-primary`, `--c-text`, `--c-surface`, `--c-surface-2`, `--c-bg`, `--c-border`, and `--c-scrim` are used.

Still pending for this addendum:

1. `npm run build` and `npm run generate` now pass in the GitHub `Frontend checks` workflow.
2. Open `/ui-kit` and visually review all 100 combinations at desktop and mobile widths.
3. Tune any module-specific spacing found during rendered QA; do not change `v1`.
4. Public `gh-pages` remains unchanged.


## Addendum: operational campaign imagery

Files:

- `frontend/app/config/operational-media.ts`
- `frontend/app/config/mock/home.ts`
- `frontend/app/components/AppBanner.vue`
- `frontend/app/components/AppBannerV2.vue`
- `frontend/app/components/AppBannerV3.vue`
- `frontend/app/components/CategoryHero.vue`
- `frontend/app/components/CategoryHeroV2.vue`
- `frontend/app/components/CategoryHeroV3.vue`
- `frontend/app/components/home/Promotion.vue`
- `frontend/app/components/home/PromotionV2.vue`
- `frontend/app/components/home/PromotionV3.vue`
- `frontend/app/pages/hot-games.vue`
- `frontend/app/pages/sport.vue`
- `frontend/app/pages/live.vue`
- `frontend/app/pages/slot.vue`
- `frontend/app/pages/fish.vue`
- `frontend/app/pages/mini-games.vue`
- `frontend/app/pages/promotion.vue`
- `frontend/app/pages/ui-kit.vue`
- `frontend/app/assets/css/main.css`
- `.github/workflows/frontend-checks.yml`

Completed behavior:

- Homepage banners now use three simulated operating campaigns:
  - esports weekly cashback
  - premium VIP membership
  - crypto/USDT deposit promotion
- The seven top-level category pages now use photographic, full-bleed campaign heroes:
  - Hot Games
  - Sports
  - Live
  - Slots
  - Fish
  - Mini Games
  - Promotion
- Promotion cards and promotion detail pages now use campaign-specific casino, slots, luxury, and gaming media.
- All campaign copy remains HTML text over decorative images, so headings and calls to action remain accessible and editable.
- Every image has a configured focal point for stable desktop/mobile cropping.
- Failed remote images hide cleanly and leave the token-based fallback background visible.
- Scrims, borders, and hover treatments use the existing semantic color tokens to preserve all site skins.
- The existing v1-v10 UI Kit variants inherit the new media without duplicating content data.
- External image URLs and their source page references are centralized in `operational-media.ts`.

Relevant commits:

- `f50935f` Add operational campaign media config
- `7c76f59` Add realistic homepage campaign content
- `ef0e175`, `fe1bcf2`, `918019e` Integrate banner media across v1-v3
- `c919e48`, `a00d825`, `294193b` Integrate category hero media across v1-v3
- `9b9b83f`, `2a9aa1e`, `1dfd788`, `6eb3ed0`, `6aa9441`, `2e81cdf`, `aa808eb` Apply media to category and promotion pages
- `bb59497`, `a6856bb`, `b1c2e7a` Apply media to home promotion variants
- `fdfc037` Add semantic operational media treatments
- `46e58ff` Update UI Kit category hero preview
- `cc03cac` Add GitHub frontend validation workflow

Verification:

- GitHub Actions run `29497335722` completed successfully.
- `npm ci`, `npm run build`, and `npm run generate` all passed.
- Re-fetched the modified promotion and banner templates from `main` and checked media fallbacks, focal points, scrim layering, and detail routing.
- No local checkout was used.
- No deployment job was added and `gh-pages` remains unchanged.

Still pending:

1. Rendered visual QA at desktop and mobile viewports after a preview or deployment target is approved.
2. Final art-direction review of image crops and campaign copy.
3. Downloading approved photos into the repository if the production policy should avoid third-party CDN dependencies.
4. Public deployment remains blocked on an explicit owner decision; do not change `gh-pages` automatically.


## Addendum: live casino art direction

Files:

- `frontend/app/config/operational-media.ts`
- `frontend/app/components/VendorBrowser.vue`

Completed behavior:

- The Live Casino category hero now uses a real roulette dealer and table scene instead of a generic luxury portrait.
- The Live Casino vendor selection cards now use dealer/table photography with a `LIVE STUDIO` status.
- Games inside a selected Live Casino vendor now cycle through dedicated roulette, baccarat, blackjack, and card-dealing photos.
- Placeholder `Game Name` labels are replaced for Live Casino with realistic table titles such as `Lightning Roulette`, `VIP Baccarat`, `Speed Blackjack`, and `Dragon Tiger`.
- Each live game card shows a `LIVE DEALER` indicator and preserves the existing site tokens, hover behavior, provider flow, Back logic, and Load More behavior.
- Slot and other category imagery remains unchanged.

Relevant commits:

- `f150410` Add live dealer campaign media
- `b01ce73` Style live casino with dealer imagery

Verification:

- GitHub Actions run `29498426636` completed successfully.
- `npm ci`, `npm run build`, and `npm run generate` passed.
- The public `gh-pages` branch remains unchanged.


## Addendum: admin image size guidance

File:

- `frontend/app/pages/admin.vue`

Completed behavior:

- Every image file upload in `/admin` now shows a visible recommended size and aspect ratio.
- Homepage Banner: `1920 × 640 px` (`3:1`).
- Promotion card: `1200 × 450 px` (`8:3`).
- Hot Game cover: `900 × 1200 px` (`3:4`).
- Mini/Slot/Live catalog image: `800 × 800 px` (`1:1`).
- Image URL fields for newly added games show the same guidance.
- File pickers now explicitly accept JPG, PNG, and WebP.
- Guidance is visible below each control and repeated in its hover title.

Relevant commit:

- `d3990a2` Show recommended sizes for image uploads

Verification:

- Confirmed all four `type="file"` controls reference a matching size guide.
- GitHub Actions run `29498948326` completed successfully.
- `npm ci`, `npm run build`, and `npm run generate` passed.
- `gh-pages` remains unchanged.


## Addendum: World Football 2026 homepage banner

Files:

- `frontend/app/config/operational-media.ts`
- `frontend/app/config/mock/home.ts`
- `frontend/app/components/AppBannerV3.vue`

Completed behavior:

- Added a World Football 2026 campaign as the first homepage carousel slide.
- Campaign copy:
  - badge: `WORLD FOOTBALL 2026`
  - title: `Road to Glory`
  - highlight: `2026`
  - subline: `LIVE ODDS · MATCH CENTRE · EVERY KICK`
  - CTA: `View Matches`
- Uses a packed night soccer stadium image with crowd energy and match lighting.
- The copy deliberately avoids official FIFA marks and remains editable HTML over the image.
- Existing esports, VIP, and crypto finance slides remain available.
- AppBanner v3 now uses dynamic auto rows so four or more carousel thumbnails remain correctly arranged.

Relevant commits:

- `a935fdf` Add world football campaign media
- `f1a69e5` Add world football homepage banner
- `e065752` Support dynamic banner thumbnail count

Verification:

- Confirmed all three changes exist on the current `main` head.
- GitHub Actions run `29499467657` completed successfully.
- `npm ci`, `npm run build`, and `npm run generate` passed.
- `gh-pages` remains unchanged.


## Addendum: Fashion Blue skin

Files:

- `frontend/app/assets/css/themes/fashion-blue.css`
- `frontend/nuxt.config.ts`
- `frontend/app/config/template.ts`
- `frontend/app/utils/themes.ts`
- `frontend/app/pages/admin.vue`
- `frontend/app/pages/studio/index.vue`

Completed behavior:

- Added a fourth selectable skin with key `fashion-blue` and label `Fashion Blue`.
- The palette combines graphite-black surfaces, electric-blue primary actions, ice-blue typography, and cyan highlights.
- Champagne gold, mint green, and coral red remain available for status and emphasis, so the interface is not a one-note blue palette.
- The skin is loaded by Nuxt and is available through the front-end skin switcher, the client `/admin` skin controls, and the `/studio` design controls.
- Admin and Studio display the branded label `Fashion Blue` instead of the raw theme key.
- Existing components continue to use semantic color variables; no page structure or content behavior was changed.

Relevant commits:

- `80ec090` Add Fashion Blue theme tokens
- `2b79b39` Load Fashion Blue theme
- `8d11896` Allow Fashion Blue in client skin controls
- `ddce6b2` Label the Fashion Blue skin
- `d7aca38` Show branded skin names in admin
- `6d240cb` Show branded skin names in studio

Verification:

- Re-fetched all six changed files from GitHub `main`.
- Confirmed the CSS token set, Nuxt registration, allowed skin key, and branded labels.
- GitHub Actions run `29499976587` validates the final code head; record its final result below when complete.
- No local checkout was used.
- Public `gh-pages` remains unchanged.
