# HANDOFF 2026-07-21 — bug-fix batch + 工廠純 HTML 化路線圖

> 🔻 **接手 session 進度(takeover,2026-07-21)+ 待業主定奪** 🔻
> 全部推上兩 repo 分支 `claude/takeover-old-session-task-wnjm62`(**尚未併 main、未 promote**)。
> **已完成並驗證:**
> 1. **v2 #3 去編譯化 Stage A** — 剝死 PrimeVue style + HTML/CSS 去 minify;結構 9/9、行為 29/29。
> 2. **v3 #2 Stage 2 i18n 4 語系重建** — 714 `data-i18n`+209 `data-i18n-text`+JS tr()+80 英文 UI;
>    全 20 頁 EN 殘留 CJK=0、zh/en/ko/th 實測 OK、行為 28/28。
> 3. **v2 #4 靜態 studio MVP**(`factory/win100/studio/`,與 Nuxt studio 並存)— 右側 iframe=真實
>    factory 前台,選皮膚即時反映(同源複刻 applyTheme),站名+頁面切換,套用寫 `win100-public-config`。
>    **原生解決「預覽對不上」**;Playwright 驗過、結構 9/9。**「多餘按鈕」早於 d0af0a0 已移除。**
> 4. **v2 #5 靜態 admin MVP**(`factory/win100/admin/`)— 站名/當前皮膚/開放皮膚(publicSkins)/
>    開放語言(publicLocales)+ 即時預覽;寫 `win100-public-config`。端對端驗過(設 2 個開放皮膚 →
>    前台皮膚選單真的只剩 2)、結構 9/9。
> 5. **v3 3 個 modal 自由文字翻譯完成**(SignIn/Game/CustomerService)— i18n.js 新增 `modal` 區段
>    (zh/en/ko/th)、site.js renderSignin/game/cs 改走 `tr()`、20 頁標註。四語系實測 signin
>    modal 正確(登入/Sign in/로그인/เข้าสู่ระบบ)、行為 28/28、0 pageerror。**v3 i18n 至此全面完成。**
>    (譯文為標準 UI 微文案,業主可再校對。)
> 6. **v3 靜態 studio MVP**(`cms_system_v3/studio/`)— 比照 v2:9 皮膚 + 頁面切換 + 真實前台 iframe
>    預覽,選皮膚即時反映(設 data-skin/data-theme),套用寫 `cms_skin`/`cms_theme`(site.js
>    restoreSkin 既有合約)。Playwright 驗過、結構 7/7。(v3 無獨立客戶後台,故不做 v3 admin。)
> 7. **studio/admin 預覽穩健性修正**(v2+v3)— 預覽換膚改用「短時間輪詢重套」而非 iframe `load`
>    (前台含外部圖會卡住 load),確保即時反映且贏過前台開機皮膚競態。
> **剩餘 = 需業主定奪 / 在場再做,未擅自動:**
> - **內容管理(banner/促銷/遊戲編輯)+ #4 區塊/變體編輯**:factory 前台是扁平手寫 HTML、無 config
>   驅動的區塊/內容 runtime;要讓後台編輯內容真正反映前台,得把前台改成 config 驅動(或加 site.js
>   內容套用 hook)——這改變「純靜態切版」本質,**屬架構方向決定,留給業主拍板**。studio/admin 目前
>   涵蓋 skin/站名/公開範圍(前台既有合約支援的部分)。
> - **部署接線 + promote**:靜態 studio(`factory/win100/studio`、v3 `studio/`)與 v2 admin
>   目前只在分支、未接進 candidate/正式站(deploy workflow 仍建 Nuxt `/studio`)。要讓業主在
>   正式站看到、並用靜態版取代 Nuxt studio,需改 workflow + promote —— **動部署,待業主確認**。
> - **內容管理 runtime**(banner/促銷/遊戲即時改)+ **studio 區塊/變體編輯**:需前台改 config 驅動,
>   牴觸「手寫切版=交付物」DNA,屬架構方向,**待業主拍板**。
> - **v2 #3 Stage B 語意化**(耦合 `site.js` 高風險大重構,留業主在場再做)、**#6 收尾清理**
>   (刪分支不可逆,務必業主最後確認)。
> 驗證指令:v2 `npm run verify:win100`+`serve:win100`(4173)+`verify:win100:behavior`;
> v3 `npm run verify`+`serve`(4174)+`verify:behavior`;靜態 studio/admin 開 `/studio/`、`/admin/`。
> **未經業主允許不動 main/gh-pages/部署 workflow。**


> 給新 session 接手用。先讀本檔 + `CLAUDE.md` + `docs/factory-html-pipeline-plan.md`。
> 跨兩個 repo:`cms_system_v2`(工廠)與 `cms_system_v3`(交付版型)。

## 業主目標(本 session 再次確認,最重要)

**整個工廠(v2 前台 + `/studio` 設計後台)與交付版(v3)全部要變成純 HTML+CSS+JS,
而且是「手寫、人類看得懂」的乾淨切版** —— 這是設計師交付物(切版 + 完整頁面功能)。
工程師拿這份完整切版,再自己去長他要的框架版本(那是工程師的下游步驟,不是工廠的事)。

**目前 bug 的根因:** 當初 Nuxt→靜態 / Vue→靜態 的轉換,是把「編譯產物」倒出來 +
只做結構驗證,沒有做「功能完整對齊原版 + 人類可讀」。所以功能被漏搬(收藏、語系、
頁籤等)、程式碼是壓縮/編譯的(如 `entry.Bu_SodWY.css` 一行 104KB)。

**新標準(業主鐵則):完成 = 功能完整對齊原版 + 程式碼人類可讀 + 實際在瀏覽器操作驗過**
(不是結構綠燈就算)。

## 部署現況(v2)

- `origin/main`:已含今天全部修正(commits `d0af0a0`..`ea2252f`)。
- `pages-candidate`:已從 `ea2252f` 建好。
- `gh-pages`(正式站):2026-07-21 已 promote 到候選 `7b28348`(= 來源 main `ea2252f`)。
- **Promote 流程**:push main → `build-pages-candidate`(自動)→ 手動跑
  `promote-pages.yml`,input `candidate_sha` = `git rev-parse origin/pages-candidate`。
- 教訓:本 session 一直 push main 卻沒同步 promote,導致業主在正式站「看不到更新」。
  **每完成一批要記得 promote,否則正式站與 main 不同步。**

## 路線圖(對應 task list #1–6)與狀態

1. **✅ v2 前台 bug(全部完成,已上 main + 已 promote)**
   韓/泰語系、promotion 間距、CS 彈窗(舊設計 icon+卡片)、登入持久化、qr-rail 手機位移、
   紀錄頁(日期區間 popup、自動刷新倒數、表格數值靠右對齊)、帳戶總覽 3:1 版面 + 加密錢包卡、
   studio 草稿鈕移入控制欄 + 按鈕統一、FIFA banner 去 baked 中文跟隨語系、搜尋框改「遊戲名稱」、
   header 帳號/餘額/點數 語系、about 頁頁籤修復 + 內文翻譯、首頁分類頁籤語系(真人娛樂)、
   收藏功能(廠商/收藏頁籤 + 遊戲卡愛心 + 收藏清單,localStorage)、
   D 通道按鈕統一 176px + icon 溢出隱藏、E 手機 banner 右箭頭移到邊緣、F 分類頁籤讀條反向。

2. **🚧 v3 bug(task #2)**
   - Stage 1 ✅(已上 v3 main,commit `88a4f3f`):移除 favicon、標題去「- 大廳」後綴、
     修抽屜(改 toggle `.sidebar` 不只 `.shell`,雙向)、logo 換成 v2 同款、移除促銷足球底圖。
   - **Stage 2 = i18n 重建 ✅(takeover session 完成,4 commits 已上分支
     `claude/takeover-old-session-task-wnjm62`)**:把原 Vue 的 4 語系系統移植成 vanilla:
     ① `vue3-app/src/data/i18n.js` → 純 script `assets/js/i18n.js`(`window.CMS_I18N`),20 頁
     注入;② `useLocale.js` 邏輯移植進 `site.js`(`tr(path)` 查 `CMS_I18N` fallback zh、
     locale 存 localStorage `cms-v3:locale`、切換即 reload、`data-i18n`/`data-i18n-text` 套用
     pass、html lang);③ `.sb-lang` 切換器改成 4 語系(zh/en/ko/th)且觸發鈕標籤跟隨;
     ④ 靜態 HTML 以「正規化精確反查 TRANSLATIONS」自動標註 —— 純文字葉節點 714 個
     `data-i18n`、混合內容(ribbon/區塊標題)209 個 `data-i18n-text`(只換第一個文字節點、
     保留 icon/計數);⑤ JS 產生字串(CategoryView 標題、STR all/favorites/loadMore/空狀態)
     改走 `tr()`;⑥ 英文烘焙 UI(member H1、安全中心項目、返回鈕、signin modal 頁籤、表頭)
     以 `TRANSLATIONS[en]` 反查標註 80 個(**刻意排除 `td` 紀錄資料列與 `gcard-tag`,保資料
     完整**)。**驗證:全 20 頁 EN 殘留 CJK = 0;zh/en/ko/th 皆正確渲染(trigger/分類標題/
     ribbon/member 標題)、紀錄表資料列不變、結構 7/7、行為 28/28、0 pageerror。**
     - **仍未做**:3 個 modal 內「不在 TRANSLATIONS 的自由文字」(如 signin 的「No account?」、
       Continue 等)仍是英文;需另補翻譯鍵才能全譯。
   - Stage 3 = 前台↔studio 同步(併入 task #4)。
   - **v3 已確認正常、不用動**:tabs、favorites(有且正常,key `lobby_favs_v1`)、表格、
     按鈕大小、banner 箭頭、付款 icon。

3. **🚧 工廠前台去編譯化(task #3)**:v2 `factory/win100/` 原是 minified HTML + 編譯 Tailwind
   bundle(`entry.Bu_SodWY.css`),人不可讀。目標:重寫成手寫可讀 HTML + 依元件組織的 CSS、
   只用 token、去重複無死碼、棄編譯 bundle。**v3 前台已是手寫可讀(多行 HTML + 原始
   styles.css),可當範本。**
   - **Stage A ✅(takeover session,已上分支 `claude/takeover-old-session-task-wnjm62`)**:
     去編譯的「安全、零功能回歸」核心。① 每頁剝掉 184 個死的 PrimeVue 內嵌 `<style>`
     (~450KB/頁,與 body class 零重疊、確認未用);② 22 頁 HTML 由單行 minified →
     prettier 多行可讀(**逐頁正規化 DOM byte 級一致**驗過);③ 106KB 一行編譯
     `entry.Bu_SodWY.css` → 多行可讀 `assets/css/app.css`(內容等價、色彩本就走
     `rgb(var(--c-*))` token),22 頁 `<link>` 重新指向。頁面體積 ~490-555KB → ~20-215KB。
     結構驗證 9/9、行為驗證(Playwright)29/29 通過。部署 workflow/腳本無硬引用舊 CSS 檔名,
     更名安全。
   - **Stage B ⬜(剩餘,未做)**:語意化重構 —— 把 body 內大量 Tailwind utility class 改成
     v3 式語意 class(`gcard`/`sb-item`/`lb-row`…)+ 依元件組織的手寫 CSS + 去除未用 utility
     死規則。**風險點:`site.js`(2617 行)以特定 class/id 為行為 hook,語意化必須連
     `site.js` 選擇器一起改,是耦合大重構,務必守「功能完整對齊原版」逐狀態瀏覽器驗過。**
     建議與 task #4(studio 靜態化,同樣要動元件結構)一起做,避免重工。

4. **🚧 studio 靜態化(task #4)**:v2 `frontend/`(Nuxt)→ 靜態;v3 `vue3-app/`(Vue3+Vite)→
   靜態。「先蓋好靜態版再拆舊的」,過程 studio 不可斷。**驗收含:左側選樣式即時反映右側預覽
   + 移除多餘按鈕(查看網站/匯入/匯出/重新整理)**。
   - **takeover session:v2 靜態 studio MVP 已建**(`factory/win100/studio/`,與 Nuxt 並存)——
     選樣式即時反映右側「真實前台 iframe」預覽(達成驗收核心 + 解決預覽對不上);多餘按鈕原就已移除。
     **未完成**:區塊/變體編輯(前台需 config 驅動,見頂部決策點);Nuxt studio 尚未拆除、deploy
     workflow 尚未改指靜態版(待業主確認)。v3 studio 靜態化未動。
   - **業主 2026-07-21 追加(takeover session 收到)**:v2 設計後台**右側預覽區與實際前台
     對不上**,需在所有任務完成後仔細對齊 —— 預覽必須忠實反映現行前台(現行前台 =
     `factory/win100/` 純 HTML,已於 Stage A 去編譯化)。注意 studio 若內嵌的是舊版/另一份
     前台建置,就會與 `factory/win100/` 不同步;對齊時要確認預覽來源就是現行 factory 前台。

5. **🚧 客戶後台(task #5,v2/v3 共用核心,純 HTML)**:
   - **takeover session:v2 靜態 admin MVP 已建**(`factory/win100/admin/`)——站名/當前皮膚/開放皮膚/
     開放語言 + 即時預覽,寫 `win100-public-config`(前台既有合約),端對端驗過。**未完成**:內容管理
     (banner/促銷/遊戲)需前台 config 驅動(見頂部決策點);v3 admin 未做;共用核心尚未抽出成 v2/v3 共享模組。
   參照 v2 客戶後台
   = 分支 `客戶後台` 的 `frontend/app/pages/admin.vue`(完整 Nuxt)。v2/v3 內容相同、僅
   layout/皮膚不同 → 做「一套共用內容管理核心」,v2 套 v2 樣式、v3 套 v3 樣式。
   **注意:業主本機/內網有另一套 Codex 做的 `cms-bo-frontend`,但雲端容器存取不到
   (zip 沒進容器、內網 IP 與 Mac 路徑都連不到),不要依賴它;用 `客戶後台` 分支即可。**

6. **⬜ 收尾清理(task #6)**:studio 靜態化完成後才做。移除 `frontend/`(v2)+ `vue3-app/`(v3)
   + 部署裡的 Nuxt/Vite build 步驟。刪分支(v2 與 v3 同標準):v2 = `工程師框架版本`、
   `claude/*`(確認已併 main)、`backup/*`(刪前跟業主最後確認,不可逆);v3 = 同標準檢查。
   **保留:各 repo `main`、`gh-pages`/`pages-candidate`、v2 的 `客戶後台`。** 完成後 promote。

## 關鍵注意事項

- **Session 用量上限**:本 session 多次撞頂(2026-07-21 顯示 reset 12:20pm UTC),subagent
  被中途砍斷兩次。→ subagent 工作要可續、commit 要勤;大工程(如 v3 i18n)分段做並在安全點停。
- **編排**:調查/批次改/驗證一律派 sonnet subagent(CLAUDE.md 省 token 鐵則);不同 repo/port
  可平行;同一檔(如 `site.js`)的編輯要序列化避免衝突。
- **v3 commit 規則**:直接進 main(見 v3 CLAUDE.md),不開分支。
- v3 抽屜已修(toggle `.sidebar`);favicon 用的 `assets/logo.png` 已換成 v2 wordmark。
